#include "shell.h"

/**
 */
char *_strtok(char *buffer, const char *delimter)
{
	/* create a function that works like strtok()
	 *
	 * man for more info
	 *
	 * If buffer is not NULL, start fromthe begging of the buffer
	 * but if buffer is null, the function starts from the last position you stopped at
	 *
	 * return value -----> The strtok() function return a pointer to the next token
	 * , or NULL if there are no more tokens.
	 */
}
